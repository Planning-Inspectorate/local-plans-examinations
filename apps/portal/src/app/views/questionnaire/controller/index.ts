/**
 * Controller module exports for questionnaire functionality.
 * Provides factory functions for creating questionnaire page handlers and submission controllers.
 */

import { createQuestionnaireControllers } from './controller.ts';
import { createSaveController } from './save.ts';

export { createQuestionnaireControllers, createSaveController };
