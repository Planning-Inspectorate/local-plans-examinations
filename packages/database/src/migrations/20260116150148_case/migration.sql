BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[Case] (
    [id] NVARCHAR(30) NOT NULL,
    [name] NVARCHAR(250) NOT NULL,
    [gateway1EstimatedDate] DATE NOT NULL,
    CONSTRAINT [Case_pkey] PRIMARY KEY CLUSTERED ([id])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
